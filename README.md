# Tulasi Ram — Portfolio

React + Vite portfolio site. Design follows the Figma file (header + landing page + scroll navigation); copy is sourced from `portfolio-copy-rewrite.md`.

## Develop

```bash
npm install
npm run dev
```

## Before publishing

All editable copy lives in `src/data/content.js`. One thing is still outstanding — search that file for `TODO`:

- Project `codeUrl` / `demoUrl` links (a project's action buttons only render once a URL is filled in)
